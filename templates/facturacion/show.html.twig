{% extends 'base.html.twig' %}

{% block title %}Facturacion{% endblock %}

{% block body %}
    <h1>Facturacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdFacturacion</th>
                <td>{{ facturacion.idFacturacion }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ facturacion.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_facturacion_index') }}">back to list</a>

    <a href="{{ path('app_facturacion_edit', {'idFacturacion': facturacion.idFacturacion}) }}">edit</a>

    {{ include('facturacion/_delete_form.html.twig') }}
{% endblock %}
