<form method="post" action="{{ path('app_facturacion_delete', {'idFacturacion': facturacion.idFacturacion}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facturacion.idFacturacion) }}">
    <button class="btn">Delete</button>
</form>
