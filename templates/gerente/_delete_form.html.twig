<form method="post" action="{{ path('app_gerente_delete', {'idGerente': gerente.idGerente}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gerente.idGerente) }}">
    <button class="btn">Delete</button>
</form>
